[pytest]
markers =
    osrm: tests que requieren un servidor OSRM real corriendo en localhost (ver docker/docker-compose.yml)
    artefactos: tests que requieren los artefactos de Fase 4 (python run_fase4.py)
